#!/bin/bash

function copyFiles() {
    if [ -d $1 ]; then
        rm $1 -rf
    fi

    mkdir $1
    cp ${@:2} $1
}

copyFiles krita $HOME/.config/krita*
copyFiles ssh $HOME/.ssh/*

FOLDERS=(
    "ssh"
    "krita"
)

tar -czvf ~/backup.tar.gz "${FOLDERS[@]}"

rm krita -rf
rm ssh -rf
