#!/bin/bash

SOURCE=/home/working/wine-source/patch

cd $SOURCE
git pull origin master

cd ../wine64
$SOURCE/configure --enable-win64
make

cd ../wine32
PKG_CONFIG_PATH=/usr/lib32/pkgconfig $SOURCE/configure --with-wine64=../wine64
make
